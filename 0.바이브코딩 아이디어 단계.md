교육기관 특화 차세대 AI 솔루션 Canva 프레젠테이션 기획안
본 기획안은 IT 취업 부트캠프(사교육) 현장의 실질적 수요를 타겟팅한 솔루션입니다. 특정 프레임워크에 종속되는 대신, 자율형 다중 에이전트 시스템에서 널리 활용되는 최신 오케스트레이션 아키텍처와 기술 스택(Heartbeat, Runtime Skill Injection, MCP 등)을 시스템에 직접 이식하여 설계되었습니다.

Main Title: K-디지털 트레이닝 차세대 AI 교육 솔루션

Sub Title: 다중 에이전트(Multi-agent) 오케스트레이션 기반 학원 자율 운영 생태계

아키텍처 핵심 기술 스택 및 구동 메커니즘
기술 스택: Node.js 20+, React UI, 내장 PostgreSQL, Docker Compose 환경.

Heartbeat 스케줄링 로직: 챗봇처럼 사용자의 명령을 기다리거나 24시간 연속으로 켜져 있어 API 비용을 낭비하지 않습니다. 에이전트는 정해진 주기(Heartbeat)마다 깨어나 현재 상태를 스캔하고 작업을 마친 뒤 다시 휴면 상태로 돌아가 비용을 최적화합니다.

MCP (Model Context Protocol) 연동: 에이전트가 학원의 로컬 LMS, 사내 GitHub 리포지토리, 출결 시스템 등 외부 데이터에 안전하게 직접 접근할 수 있도록 표준화된 플러그인 프로토콜을 사용합니다.

기능 1. Runtime Skill Injection 기반 실무 AI 강사
문제점: "구글링 하세요" 식의 방치형 코딩 피드백. 반면 학원마다 사용하는 고유 프레임워크가 달라 범용 AI로는 한계가 있음.

구현 메커니즘: 값비싼 모델 파인튜닝(Fine-tuning)을 진행하지 않습니다. 베테랑 강사들의 코딩 컨벤션이나 코드 리뷰 노하우를 단순한 마크다운(.md) 파일로 작성해 두면, 시스템이 런타임(Runtime) 환경에서 이를 에이전트의 'Skill'로 즉시 주입합니다.

기대 효과: 다음 달에 자바(Java) 반에서 파이썬(Python) 반으로 수업이 바뀌어도, 스킬 파일 교체 하나만으로 AI 강사의 전문성이 완벽히 전환됩니다.

기능 2. Heartbeat 스케줄링 기반 중도탈락 위험 예측 (EWS)
문제점: 복잡한 중도탈락 조건으로 인한 국비 지원금 누수 방지 및 선제적 관리 부족.

구현 메커니즘: 위험 관리 에이전트가 1시간, 혹은 하루 단위의 'Heartbeat' 주기에 맞춰 자율적으로 기상합니다. 깨어난 에이전트는 PostgreSQL 데이터베이스에 쌓인 수강생의 출결 내역, 과제 미제출 이력, 강사 상담 일지를 스캔하여 이탈 위험군을 식별한 뒤 다시 수면 모드로 전환합니다.

기대 효과: 임계치가 넘는 수강생 발견 시 즉시 원장의 Slack으로 경고 알림을 발송하거나, 멘탈 케어 에이전트가 학생에게 자동 안부 메시지를 건네 골든타임을 확보합니다.

기능 3. MCP 및 RAG 결합형 전용 AI 튜터
문제점: 범용 AI에 질문할 경우 학원 커리큘럼이나 교재 범위를 벗어난 엉뚱한 답변(환각 현상) 제공.

구현 메커니즘: MCP(Model Context Protocol)를 활용해 학원의 내부 시스템을 AI와 직접 연결합니다. 학원 자체 PDF 교재나 역대 기출 코딩 문제를 벡터(Vector) DB에 임베딩하는 RAG(검색 증강 생성) 아키텍처를 도입합니다.

기대 효과: 학생이 에러를 질문하면, AI는 즉시 정답 코드를 주는 대신 "교재 50페이지의 A 개념을 활용해 반복문 조건을 어떻게 수정할 수 있을까요?"라며 학원 커리큘럼에 기반한 소크라테스식 문답을 유도합니다.

기능 4. 다중 에이전트 기반 포트폴리오 차별화(Differentiation) 시스템
문제점: 수강생 전원이 똑같은 '게시판', '쇼핑몰' 클론 코딩을 제출하여 취업 시장에서 경쟁력 상실.

구현 메커니즘: 여러 AI 에이전트(기획자, 보안전문가, 고객 페르소나 등)가 서로 목표(Goal)를 인식하고 협업하는 다중 에이전트 구조를 사용합니다.

1단계 (페르소나 인터뷰): AI가 특정 산업군의 '고객' 역할을 맡아 수강생과 요구사항을 도출하는 인터뷰를 시뮬레이션하여 고유의 기획안을 끌어냅니다.

2단계 (유사도 판별 피드백): 학생이 기획서를 제출하면, 평가 에이전트가 이전 수료생들의 프로젝트와 유사도를 분석하여 "이 구조는 85% 중복됩니다. 결제 모듈에 블록체인을 결합하여 차별화하세요"라는 피드백을 지속해서 제공합니다.


---

## AI 검토 의견 (GitHub Copilot)

### 전반적 평가
본 기획안은 단순한 챗봇 도입 수준을 넘어, 실제 자율 운영 생태계를 목표로 정교하게 설계된 점에서 높이 평가됩니다. 특히 EWS(중도탈락 예측)와 Runtime Skill Injection 조합은 국비지원 훈련기관의 고유 고통 지점(Pain Point)을 정확히 겨냥하고 있습니다.

### 강점

1. **비용 효율적 설계**: Fine-tuning 대신 Runtime Skill Injection을 채택한 것은 현실적으로 탁월한 선택입니다. 학원 커리큘럼 변경 주기(보통 3~6개월)에 맞춰 `.md` 파일 교체만으로 AI 전문성을 즉시 전환할 수 있어 운영비와 유지보수 부담이 현저히 낮습니다.

2. **Heartbeat 아키텍처의 적합성**: 학원 환경은 실시간 스트리밍 데이터가 아닌 배치성 데이터(출결, 과제 제출)가 주류입니다. 상시 가동 대신 주기적 기상 방식은 이 환경에 최적화된 구조이며, API 비용 절감 효과가 실질적입니다.

3. **포트폴리오 차별화 시스템**: 취업 시장에서 클론 코딩 포트폴리오가 갖는 한계를 명확히 짚었습니다. 유사도 판별 피드백 루프는 수료생 취업률 향상에 직접적으로 기여할 수 있어 학원 입장에서 강력한 마케팅 포인트가 됩니다.

### 보완이 필요한 지점

1. **개인정보보호법 준수 방안 명시 필요**: 수강생의 출결, 상담 일지, 과제 이력 등 민감 데이터를 PostgreSQL에 집중 저장하고 AI가 분석하는 구조이므로, 개인정보보호법 및 교육부 훈련기관 데이터 관리 지침 준수 방안(익명화, 접근 제어, 보존 기간 등)을 기획안에 포함시켜야 합니다.

2. **EWS 알림의 오탐(False Positive) 관리**: 임계치 기반 자동 알림은 데이터가 부족한 초기 운영 시 오탐율이 높을 수 있습니다. 원장 및 강사가 피드백을 통해 모델을 점진적으로 보정하는 **Human-in-the-loop** 메커니즘을 초기 단계부터 설계에 포함하는 것을 권장합니다.

3. **MCP 보안 경계 정의**: MCP로 사내 GitHub 리포지토리 및 LMS에 직접 접근하는 구조는 편리하나, 에이전트별 접근 권한 범위(Read-only vs Read-Write)와 인증 방식(OAuth, API Key 로테이션 등)을 명확히 문서화해야 보안 취약점을 방지할 수 있습니다.

4. **단계별 MVP 로드맵 부재**: 4가지 기능이 동시에 구현 목표로 제시되어 있으나, 현실적인 개발 우선순위 설정이 필요합니다. 제안 순서: **기능 3(AI 튜터) → 기능 2(EWS) → 기능 1(AI 강사) → 기능 4(포트폴리오)** 순으로 MVP를 구성하면 초기 도입 학원에서 빠른 가시적 성과를 확인할 수 있습니다.

### 기술 구현 제언

- RAG 벡터 DB는 초기에는 `pgvector`(PostgreSQL 확장)를 활용하면 별도의 벡터 DB 인프라 없이 기존 PostgreSQL 스택 안에서 통합 운영이 가능하여 Docker Compose 구성이 단순해집니다.

### 결론
국비지원 훈련기관이라는 명확한 버티컬(Vertical) 시장을 타겟으로, 현장 문제를 최신 AI 아키텍처와 연결한 기획력이 돋보입니다. 위의 보완점(특히 개인정보 및 보안 설계)을 기획안에 반영한다면 실제 B2B 제안서로서의 완성도가 크게 높아질 것입니다.

---

## AI 검토 의견 (Gemini 3.1 Pro)

### 전반적 평가
주입식 교육의 한계를 극복하고 자기주도적 학습(Self-directed Learning)을 유도하는 교육 공학적 접근이 돋보이는 훌륭한 기획입니다. 특히 단일 AI 챗봇에 의존하지 않고 각자의 역할이 부여된 다중 에이전트(Multi-agent) 시스템을 도입한 것은 실무(Agile) 환경과 가장 유사한 학습 경험을 제공할 수 있는 혁신적인 시도입니다.

### 학습 경험 및 확장성 측면의 강점

1. **소크라테스식 문답법의 시스템화**: 정답을 바로 제공하지 않고 RAG를 기반으로 교재의 맥락을 짚어주는 접근(기능 3)은 수강생의 문제 해결 능력을 기르는 데 매우 효과적입니다. 환각(Hallucination) 현상을 방어함과 동시에 학습 성취도를 높이는 이상적인 교육용 AI 패턴입니다.
2. **역할극(Persona) 기반 상호작용**: 기획자, 고객 등 다양한 페르소나 에이전트와 소통하며 프로젝트를 고도화하는 경험(기능 4)은 수강생들의 소프트 스킬(커뮤니케이션, 요구사항 분석)을 기르는 데 큰 도움이 됩니다.

### 고도화를 위한 제언 (Next Step)

1. **LLM 라우팅 및 비용 최적화(Model Routing)**: 다중 에이전트 시스템은 토큰 사용량이 급하강/증가할 수 있습니다. 단순 출결 스캔이나 문서 요약 에이전트에는 가볍고 빠른 모델을, 복잡한 코드 리뷰나 아키텍처 피드백에는 고성능 추론 모델을 동적으로 할당하는 라우팅 전략이 명시되면 좋습니다.
2. **RAG 파이프라인 자동화 전략**: 학원의 교재나 기출 문제는 주기적으로 업데이트됩니다. 문서(PDF, Markdown)가 변경되었을 때 이를 자동으로 청킹(Chunking)하고 벡터 DB에 재색인(Re-indexing)하는 데이터 파이프라인 구조를 추가로 고민해야 합니다.
3. **정량적 효과 측정(KPI) 지표 수립**: 솔루션의 성공을 증명하기 위해 "질문 해결 완료까지 걸리는 시간 30% 단축", "동일 오류 반복 질문 주기 증가" 등 AI 튜터 도입 효과를 측정할 데이터 로깅 체계를 데이터베이스 설계 시 반영할 필요가 있습니다.

### 기술 구현 제언

- 다중 에이전트 간의 워크플로우(인터뷰 → 피드백 → 승인)를 안정적으로 제어하기 위해 상태(State) 기반의 멀티 에이전트 오케스트레이션 프레임워크(예: LangGraph 또는 AutoGen)의 도입을 검토해 볼 수 있습니다. 이는 에이전트 간 무한 루프에 빠지는 현상을 방어하고 상태 추적(디버깅)을 용이하게 합니다.

### 결론
단순한 기술 과시가 아닌, 교육 현장의 실제 페인포인트(방치형 피드백, 붕어빵 포트폴리오)를 AI로 해결하려는 실용주의적 접근이 매우 뛰어납니다. 제안된 다중 모델 라우팅 체계 및 학습 평가 지표가 더해진다면 시장에서 강력한 경쟁력을 갖춘 에듀테크(EdTech) 솔루션이 될 것입니다.

---

# 종합 세부 상세 보고서

## 1. 프로젝트 개요 및 배경

### 1-1. 시장 현황 분석
국내 K-디지털 트레이닝 사업은 고용노동부가 주관하는 대규모 디지털 인재 양성 사업으로, 연간 수천억 원 규모의 국비가 투입됩니다. 그러나 현장에서는 다음과 같은 구조적 문제가 지속적으로 제기되고 있습니다.

| 문제 영역 | 현황 | 결과 |
|---|---|---|
| 학습 지원 | 강사 1인 대 수강생 20~30명 구조 | 개인 맞춤 피드백 불가 |
| 중도 탈락 | 평균 탈락률 15~25% 수준 | 훈련기관 지원금 반납 및 평가 불이익 |
| 포트폴리오 | 전국 수강생 80% 이상이 동일한 프로젝트 제출 | 취업 시장에서 변별력 상실 |
| 커리큘럼 대응 | 기술 트렌드 변화에 3~6개월 지연 대응 | 실무 역량 괴리 심화 |

### 1-2. 솔루션 포지셔닝
본 솔루션은 기존의 단순 LMS(학습관리시스템)나 범용 AI 챗봇 도입과는 근본적으로 다른 접근을 취합니다. 학원의 고유 데이터와 운영 맥락 안에서 자율적으로 작동하는 **전문화된 다중 에이전트 생태계**를 구축하는 것이 핵심입니다.

---

## 2. 시스템 아키텍처 상세 설계

### 2-1. 전체 아키텍처 구성도

```
┌─────────────────────────────────────────────────────────────────┐
│                        React UI (Frontend)                      │
│        [강사 대시보드]  [수강생 포털]  [원장 관리 콘솔]           │
└────────────────────────────┬────────────────────────────────────┘
                             │ REST API / WebSocket
┌────────────────────────────▼────────────────────────────────────┐
│                    Node.js 20+ API Server                       │
│              [인증] [라우팅] [에이전트 오케스트레이터]             │
└──────┬─────────────────────┬──────────────────────┬─────────────┘
       │                     │                      │
┌──────▼──────┐   ┌──────────▼──────────┐   ┌──────▼──────────┐
│  Heartbeat  │   │   Agent Pool        │   │   MCP Gateway   │
│  Scheduler  │   │  [AI강사 에이전트]   │   │  [LMS 커넥터]   │
│ (node-cron) │   │  [EWS 에이전트]     │   │  [GitHub 커넥터]│
│             │   │  [튜터 에이전트]     │   │  [출결 커넥터]  │
│             │   │  [포트폴리오 에이전트]│   │                 │
└──────┬──────┘   └──────────┬──────────┘   └──────┬──────────┘
       │                     │                      │
┌──────▼─────────────────────▼──────────────────────▼──────────┐
│                     PostgreSQL (+ pgvector)                    │
│  [수강생 데이터] [출결/과제] [에이전트 상태] [벡터 임베딩 DB]  │
└───────────────────────────────────────────────────────────────┘
```

### 2-2. 기술 스택 상세 명세

| 레이어 | 기술 | 버전 | 역할 |
|---|---|---|---|
| 런타임 | Node.js | 20 LTS | API 서버, 에이전트 오케스트레이션 |
| 프론트엔드 | React | 18+ | 강사·수강생·관리자 UI |
| 데이터베이스 | PostgreSQL | 16+ | 메인 데이터 저장소 |
| 벡터 확장 | pgvector | 0.7+ | RAG 임베딩 검색 |
| 스케줄러 | node-cron | 3+ | Heartbeat 주기 실행 |
| 컨테이너 | Docker Compose | v2 | 전체 환경 통합 구성 |
| AI 프로토콜 | MCP | 최신 | 외부 시스템 연동 표준 |

### 2-3. Docker Compose 환경 구성

```yaml
# docker-compose.yml 구성 예시
services:
  api:          # Node.js API + 에이전트 오케스트레이터
  db:           # PostgreSQL 16 + pgvector 확장
  ui:           # React 빌드 결과물 서빙 (Nginx)
  mcp-gateway:  # MCP 프로토콜 게이트웨이 서비스
```

단일 `docker compose up` 명령으로 전체 솔루션이 로컬 또는 온-프레미스 서버에서 즉시 구동되며, 외부 클라우드 의존도를 최소화합니다.

---

## 3. 핵심 기능 상세 명세

### 3-1. 기능 1: Runtime Skill Injection 기반 실무 AI 강사

**구동 원리:**
```
[강사가 .md 스킬 파일 작성]
        ↓
[시스템이 런타임에 스킬 파일을 로드]
        ↓
[에이전트의 System Prompt에 동적 주입 (Injection)]
        ↓
[수강생 질문 → 해당 학원·과목 맞춤형 AI 응답 생성]
```

**스킬 파일 구조 예시:**
```markdown
# Java Spring Boot 코딩 컨벤션 (코리아IT아카데미)
## 코드 리뷰 원칙
- Lombok 사용 금지, 직접 getter/setter 작성 권장
- REST API 응답 형식: ApiResponse<T> Wrapper 클래스 필수 사용
## 자주 발생하는 오류 패턴
- N+1 문제: @EntityGraph 또는 Fetch Join 유도
```

**기대 성과 지표:**
- 수강생 1인당 일 평균 질문 응답 대기 시간: 15분 → 즉시
- 강사의 반복 질문 응대 부담: 약 60% 절감 예상

---

### 3-2. 기능 2: Heartbeat 스케줄링 기반 중도탈락 위험 예측 (EWS)

**위험 점수 산출 로직:**

| 요소 | 가중치 | 임계 조건 |
|---|---|---|
| 출석률 | 40% | 최근 5일 중 2일 이상 결석 |
| 과제 미제출 | 35% | 최근 3개 과제 중 2개 미제출 |
| 강사 상담 이력 | 15% | 최근 2주 내 부정적 상담 기록 |
| AI 튜터 활용 감소 | 10% | 전주 대비 접속 빈도 50% 이하 |

**Heartbeat 주기별 작동 방식:**

```
[매 1시간 Heartbeat]
 └─ 당일 출결 데이터 스캔 → 즉각 임계치 초과 시 긴급 알림 발송

[매일 오전 7시 Heartbeat]
 └─ 전일 종합 위험 점수 산출 → 위험군 등급 분류 → 일일 리포트 생성

[매주 월요일 Heartbeat]
 └─ 주간 트렌드 분석 → 강사별 담당 학생 위험 현황 주간 보고서 발송
```

**알림 에스컬레이션 체계:**

```
위험 점수 60점 이상 → 담당 강사 Slack 알림
위험 점수 75점 이상 → 담당 강사 + 원장 Slack 알림 + 멘탈케어 에이전트 자동 안부 메시지 발송
위험 점수 90점 이상 → 전 단계 + 즉시 전화 상담 예약 자동 생성
```

---

### 3-3. 기능 3: MCP 및 RAG 결합형 전용 AI 튜터

**RAG 파이프라인 구성:**

```
[원천 데이터: PDF 교재, 기출문제, 강의노트]
        ↓
[문서 파싱 → 청킹(Chunking, 512 token 단위)]
        ↓
[임베딩 모델로 벡터 변환 → pgvector에 저장]
        ↓
[수강생 질문 → 유사도 검색(Cosine Similarity)]
        ↓
[관련 컨텍스트 + 소크라테스식 답변 프롬프트 조합]
        ↓
[커리큘럼 기반 힌트 제공]
```

**MCP 연동 대상 시스템 및 권한 범위:**

| 연동 시스템 | 접근 권한 | 수집 데이터 |
|---|---|---|
| 자체 LMS | Read-only | 강의 진도, 수강 시간, 퀴즈 점수 |
| 출결 시스템 | Read-only | 일별 출결 상태 |
| 사내 GitHub | Read-only | 과제 커밋 이력, 코드 제출 현황 |
| Slack (알림) | Write (알림 채널 한정) | 경고 알림, 리포트 발송 |

---

### 3-4. 기능 4: 다중 에이전트 기반 포트폴리오 차별화 시스템

**에이전트 역할 분담 구조:**

| 에이전트 | 역할 | 주요 행동 |
|---|---|---|
| 고객 페르소나 에이전트 | 특정 산업군 고객 역할 | 요구사항 인터뷰, 기능 우선순위 협의 |
| 기획 평가 에이전트 | 수석 기획자 역할 | 요구사항 명세서 작성, 기술 스택 제안 |
| 보안 전문가 에이전트 | 보안 컨설턴트 역할 | OWASP 기준 보안 취약점 지적 |
| 유사도 판별 에이전트 | 심사위원 역할 | 기존 프로젝트와 유사도 분석 및 차별화 제안 |

**유사도 판별 프로세스:**

```
[수강생 기획서 제출]
        ↓
[역대 수료생 프로젝트 벡터 DB와 유사도 비교]
        ↓
[유사도 85% 이상] → "구체적 차별화 요소 3가지 제안" 피드백 반환
[유사도 60~85%] → "부분 차별화 권장" 피드백 반환
[유사도 60% 미만] → "독창성 충족" 인증 및 다음 단계 진행 허가
```

---

## 4. 개발 로드맵 (단계별 MVP)

### Phase 1 — 핵심 가치 검증 (1~2개월)
- [ ] Docker Compose 기반 개발 환경 구성 (Node.js + PostgreSQL + pgvector)
- [ ] MCP 게이트웨이 기본 구조 구현 (LMS Read 연동)
- [ ] RAG 파이프라인 구축 (PDF 임베딩 → pgvector 저장 → 검색)
- [ ] **기능 3: AI 튜터 MVP 출시** (가장 즉각적인 수강생 체감 효과)

### Phase 2 — 운영 안정화 (3~4개월)
- [ ] Heartbeat 스케줄러 구현 (node-cron 기반)
- [ ] EWS 위험 점수 산출 로직 구현 및 Slack 알림 연동
- [ ] Human-in-the-loop 피드백 UI 개발 (강사가 오탐 피드백 제공)
- [ ] **기능 2: EWS 시스템 출시**

### Phase 3 — 지능화 고도화 (5~6개월)
- [x] Runtime Skill Injection 엔진 구현
- [ ] 강사용 스킬 파일 관리 UI 개발
- [ ] **기능 1: AI 강사 시스템 출시**

### Phase 4 — 차별화 완성 (7~9개월)
- [ ] 다중 에이전트 오케스트레이션 워크플로우 구현
- [ ] 유사도 판별 엔진 및 수료생 프로젝트 벡터 DB 구축
- [ ] **기능 4: 포트폴리오 차별화 시스템 출시**
- [ ] 전체 시스템 통합 테스트 및 성능 최적화

---

## 5. 보안 및 컴플라이언스 설계

### 5-1. 개인정보보호 대책

| 항목 | 적용 방안 |
|---|---|
| 데이터 최소 수집 | AI 분석에는 수강생 ID(익명 식별자)만 사용, 실명 분리 저장 |
| 접근 제어 | 역할 기반 접근 제어(RBAC): 에이전트별 DB 접근 권한 분리 |
| 저장 보안 | PostgreSQL 컬럼 레벨 암호화 적용 (민감 데이터) |
| 보존 기간 | 수료 후 5년 보존 후 자동 삭제 (교육부 지침 준수) |
| 동의 관리 | 입학 시 AI 데이터 활용 동의서 수집 및 철회 기능 제공 |

### 5-2. API 및 MCP 보안

- **MCP 인증**: 각 외부 시스템 연동 시 OAuth 2.0 또는 단방향 API Key 사용
- **API Key 관리**: 환경변수(.env) 분리 저장, 절대 코드베이스에 하드코딩 금지
- **에이전트 샌드박스**: 각 에이전트는 필요한 도구(Tool)만 호출 가능하도록 권한 범위 제한
- **감사 로그(Audit Log)**: 에이전트의 모든 외부 시스템 접근 이력 PostgreSQL에 기록

---

## 6. 기대 효과 및 성과 지표 (KPI)

### 6-1. 학원 운영 지표

| KPI | 현재(Before) | 목표(After) | 측정 방법 |
|---|---|---|---|
| 중도 탈락률 | 평균 20% | 12% 이하 | EWS 도입 후 3개월 추적 |
| 강사 반복 질문 응대 건수 | 일 평균 30건 | 10건 이하 | AI 튜터 처리율 모니터링 |
| 수강생 만족도 | 3.8/5.0 | 4.5/5.0 이상 | 종강 후 설문 |

### 6-2. 취업 성과 지표

| KPI | 현재(Before) | 목표(After) | 측정 방법 |
|---|---|---|---|
| 수료 후 6개월 내 취업률 | 55% | 70% 이상 | 수료생 사후 추적 조사 |
| 포트폴리오 독창성 점수 | 기준치 없음 | 유사도 60% 미만 달성률 90% | 유사도 판별 시스템 자동 집계 |

### 6-3. AI 운영 비용 지표

| KPI | 기존 챗봇 방식 | 본 솔루션 | 절감 효과 |
|---|---|---|---|
| 월 API 토큰 비용 | 상시 가동 기준 100% | Heartbeat 방식 약 35% | 약 65% 절감 |
| 신규 과목 전환 비용 | Fine-tuning 수백만 원 | 스킬 파일 교체 (0원) | 100% 절감 |

---

## 7. 리스크 관리

| 리스크 | 발생 가능성 | 영향도 | 대응 전략 |
|---|---|---|---|
| AI 환각(Hallucination)으로 인한 오답 제공 | 중 | 높음 | RAG로 교재 범위 고정 + 수강생에게 "AI 검증 필요" 안내 문구 표시 |
| EWS 오탐으로 인한 강사 알림 피로도 증가 | 높음 | 중 | Human-in-the-loop 피드백 수집 후 임계치 자동 보정 기능 도입 |
| MCP 연동 시스템의 API 변경으로 인한 장애 | 중 | 높음 | 어댑터 패턴(Adapter Pattern) 적용으로 연동부를 독립 모듈화 |
| 수강생 데이터 유출 | 낮음 | 매우 높음 | 개인정보 익명화 + 암호화 + 정기 보안 감사 |
| 초기 데이터 부족으로 EWS 정확도 낮음 | 높음 | 중 | Phase 2 초기 3개월은 예측 참고치로만 활용, 강사 판단 우선 |

---

## 8. 최종 종합 평가 및 권고사항

### 8-1. 이 솔루션이 갖는 차별적 가치
1. **온-프레미스 구조**: 모든 데이터가 학원 내부 서버(Docker)에서 처리되어 외부 클라우드에 수강생 데이터가 전송되지 않습니다. 이는 국비지원기관의 개인정보 규정 준수에 있어 핵심 강점입니다.
2. **락인(Lock-in) 없는 설계**: 특정 AI 제공사의 독점 서비스가 아닌 표준 프로토콜(MCP)과 오픈 스택(Node.js, PostgreSQL)을 기반으로 하여, 향후 AI 모델 교체나 기능 확장이 자유롭습니다.
3. **강사의 지식 자산화**: 베테랑 강사의 노하우가 일회성 강의로 소비되지 않고 `.md` 스킬 파일로 축적·재활용되는 지식 자산 시스템이 구축됩니다.

### 8-2. 우선 추진 권고사항
1. **Phase 1 즉시 착수**: AI 튜터(기능 3)는 별도 하드웨어 투자 없이 구현 가능하며 수강생의 즉각적인 만족도 향상을 이끌어 도입 명분을 확보할 수 있습니다.
2. **파일럿 기관 선정**: 코리아IT아카데미 내 1개 과정(예: 자바 풀스택 과정)을 파일럿으로 선정, 3개월 운영 후 KPI를 측정하여 전체 기관 확산의 근거 데이터를 확보합니다.
3. **GitHub 레포지토리 공개 관리**:  학원 특화 기능은 별도 레포지토리로 분리 관리하고 문서화를 철저히 하여 추후 타 기관 납품 시 재사용성을 확보합니다.

---


### Cron 파서 (`server/src/services/cron.ts`)


```typescript
// 지원 문법 (완전 호환)
// *        — 모든 값
// N        — 정확한 값
// N-M      — 범위
// N/S      — 스텝
// */S      — 전체 범위에서 스텝
// N-M/S    — 범위+스텝
// N,M,...  — 쉼표로 구분된 목록

parseCron("0 7 * * *")   // 매일 오전 7시 (EWS 일일 리포트)
parseCron("0 * * * *")   // 매시간 (EWS 실시간 출결 스캔)
parseCron("0 9 * * 1")   // 매주 월요일 오전 9시 (주간 보고서)
```

**`routines` 테이블 스키마** (직접 재사용):
```typescript
// packages/db/src/schema/routines.ts
{
  id, companyId, projectId, goalId,
  title, description,
  assigneeAgentId,        // 담당 에이전트
  concurrencyPolicy,      // "coalesce_if_active" | "allow_concurrent"
  catchUpPolicy,          // "skip_missed" | "catch_up"
  lastTriggeredAt,
  lastEnqueuedAt,
}

// routine_triggers 테이블
{
  routineId,
  kind,     // "cron" | "webhook" | "manual"
  enabled,
  // cron 타입일 경우 cron 표현식 포함
}
```

> **우리 프로젝트 적용 방안**: `routines` + `routine_triggers` 테이블을 그대로 재사용. EWS 에이전트의 Heartbeat 주기 설정을 UI에서 원장이 직접 변경할 수 있도록 관리자 페이지에 연결합니다.

---

### B-3. Skill Injection 시스템 (`server/src/services/company-skills.ts`)

스킬 시스템은 우리가 목표로 하는 Runtime Skill Injection의 **완성된 레퍼런스 구현체**입니다.

**`company_skills` 테이블 스키마** (직접 재사용):
```typescript
// packages/db/src/schema/company_skills.ts
{
  id, companyId,
  key,          // 스킬 고유 키 (예: "java-spring-convention")
  slug,         // URL-friendly 슬러그
  name,         // 표시 이름 (예: "Java Spring Boot 코딩 컨벤션")
  description,  // 스킬 설명
  markdown,     // 스킬 전체 내용 (강사 노하우 .md 파일 내용)
  sourceType,   // "local_path" | "github" | "package"
  sourceLocator,// 소스 경로 또는 GitHub URL
  trustLevel,   // "markdown_only" | "full"
  fileInventory,// 연관 파일 목록 (JSON)
}
```

**스킬 주입 흐름** 
```
[DB의 company_skills.markdown 조회]
        ↓
[에이전트 System Prompt 앞부분에 스킬 markdown 삽입]
        ↓
[에이전트가 해당 컨벤션·규칙을 준수하여 응답 생성]
```

**GitHub 스킬 임포트 기능** (이미 구현됨):
- `server/src/services/company-skills.ts`의 `importSkillFromGitHub()` 함수 재사용
- 강사가 GitHub에 `.md` 파일을 업로드하면 자동으로 스킬로 등록
- `sourceRef`로 Git 커밋 해시를 추적하여 버전 관리

> **우리 프로젝트 적용 방안**: `company_skills` 테이블을 `institution_skills`로 이름만 바꿔 재사용. `companyId` → `institutionId`. 강사 관리 UI에서 과목별 스킬 파일을 업로드/교체하는 기능을 추가합니다.

---

### B-4. 에이전트 조직 구조 (`packages/db/src/schema/agents.ts`)

```typescript
// 다중 에이전트 계층 구조 지원
{
  id, companyId,
  name,           // 에이전트 이름 (예: "EWS 위험 관리 에이전트")
  role,           // 역할 (예: "ews_monitor", "ai_tutor", "portfolio_reviewer")
  title,          // 직함 표시용
  status,         // "idle" | "running" | "paused"
  reportsTo,      // 상위 에이전트 FK (조직도 구성)
  adapterType,    // "process" | "http" (AI 모델 연결 방식)
  adapterConfig,  // JSON: 모델명, API Key 참조 등
  budgetMonthlyCents, // 월 예산 상한 (비용 제어)
  lastHeartbeatAt,    // 마지막 기상 시각
  permissions,        // JSON: 에이전트별 접근 권한
}
```

> **우리 프로젝트 에이전트 등록 예시:**

| name | role | reportsTo | adapterType | 비고 |
|---|---|---|---|---|
| 오케스트레이터 | orchestrator | (없음) | process | 전체 조율 |
| EWS 모니터 에이전트 | ews_monitor | 오케스트레이터 | process | 중도탈락 예측 |
| AI 강사 에이전트 | ai_instructor | 오케스트레이터 | http | 커리큘럼별 1개씩 |
| AI 튜터 에이전트 | ai_tutor | AI 강사 에이전트 | http | RAG 연동 |
| 멘탈케어 에이전트 | mental_care | EWS 모니터 | http | 자동 안부 메시지 |
| 포트폴리오 심사 에이전트 | portfolio_reviewer | 오케스트레이터 | http | 유사도 분석 |

---

### B-5. Goal 기반 목표 관리 (`packages/db/src/schema/goals.ts`)

```typescript
{
  id, companyId,
  title,        // 목표 제목
  description,  // 상세 설명
  level,        // "company" | "project" | "task"
  status,       // "planned" | "active" | "completed" | "cancelled"
  parentId,     // 계층 목표 (상위 목표 FK)
  ownerAgentId, // 담당 에이전트
}
```

> **우리 프로젝트 적용 방안**: 포트폴리오 차별화 시스템(기능 4)의 다중 에이전트 협업에서 각 에이전트가 공유 Goal을 인식하고 작업을 조율하는 데 사용합니다.

---

### B-6. 예산·비용 관리 (`server/src/services/budgets.ts`)

```typescript
// budget_policies 테이블
{
  companyId,
  scopeType,        // "company" | "agent" | "project"
  scopeId,
  thresholdType,    // "hard_limit" | "soft_alert"
  limitCents,       // 월 예산 상한 (센트 단위)
  windowKind,       // "monthly" | "daily"
}

// cost_events 테이블 (실시간 비용 추적)
{
  agentId,
  heartbeatRunId,
  inputTokens, outputTokens,
  costCents,
  model,
}
```

> **우리 프로젝트 적용 방안**: 학원별 월 LLM API 예산을 설정하고, 초과 시 에이전트를 자동 일시 정지하는 비용 거버넌스에 활용합니다.

---

### B-7. 에이전트 인증 (JWT) (`server/src/agent-auth-jwt.ts`)

```

> **우리 프로젝트 적용 방안**: 동일한 단기 JWT 패턴을 적용하여, AI 강사·EWS 에이전트가 학원 내부 API에 접근할 때 장기 API Key 노출 없이 보안을 유지합니다.

---

## C. 수정·확장이 필요한 부분

 학원 특화 도메인 데이터를 추가로 설계해야 합니다.

### C-1. 추가 설계 필요 테이블

| 테이블명 | 역할 |  테이블 |
|---|---|---|
| `institutions` | 학원 정보 | `companies` 확장 |
| `courses` | 강의 과정 (Java반, Python반) | `projects` 확장 |
| `students` | 수강생 정보 (익명 ID 포함) | (신규) |
| `attendance_logs` | 출결 기록 | (신규, MCP로 외부 연동) |
| `assignment_submissions` | 과제 제출 이력 | (신규) |
| `ews_risk_scores` | EWS 위험 점수 이력 | (신규) |
| `rag_documents` | RAG용 교재 청크 + pgvector | (신규) |
| `portfolio_projects` | 수강생 포트폴리오 기획서 | (신규) |
| `portfolio_similarity_logs` | 유사도 분석 이력 | (신규) |
| `instructor_skills` | 강사별 스킬 파일 | `company_skills` 확장 |



---

## D. 우선순위 및 구현 전략

### Phase 1 즉시 차용 목록 (복사 후 소폭 수정)

| 파일/모듈 | 차용 방식 | 수정 내용 |
|---|---|---|
| `packages/db/src/client.ts`  | `institutionId` 컬럼 추가 |
| `packages/db/src/schema/agents.ts`  | 변경 없음 |
| `packages/db/src/schema/heartbeat_runs.ts` | `institutionId` FK 추가 |
| `packages/db/src/schema/routines.ts`  | `courseId` FK 추가 |
| `packages/db/src/schema/company_skills.ts` | `companyId` → `institutionId` 리네임 |
| `packages/db/src/schema/goals.ts` | 변경 없음 |
| `server/src/services/cron.ts` | 변경 없음 (완전 독립 모듈) |
| `server/src/services/budgets.ts`  | 소폭 수정 |
| `server/src/agent-auth-jwt.ts` | 환경변수명만 변경 |

### Phase 2 참조 구현 목록 (로직 참조 후 재작성)

| 파일/모듈 | 참조 포인트 | 재작성 범위 |
|---|---|---|
| `server/src/services/heartbeat.ts` | wakeup 큐, 상태 머신, 동시 실행 제어 | EWS 도메인 로직 추가 |
| `server/src/services/company-skills.ts` | GitHub import, markdown 로드·주입 로직 | `instructor_skills` 테이블 적용 |
| `server/src/services/routines.ts` | cron 파서 연동, catchUpPolicy 처리 | Heartbeat 주기 관리 UI 연동 |
| `server/src/services/agent-instructions.ts` | 스킬 파일 번들링, System Prompt 주입 흐름 | RAG 컨텍스트와 결합 |

---

## E. 모노레포 구성 전략 (pnpm workspace 차용)


```
our-project/
├── packages/
│   ├── db/           # Drizzle 스키마 ( 학원 도메인 추가)
│   ├── shared/       # 공유 타입 및 유틸리티
│   └── rag/          # RAG 파이프라인 (신규 - pgvector 연동)
├── server/           # Node.js API + 에이전트 오케스트레이터
├── ui/               # React 대시보드
├── skills/           # 기본 스킬 파일 예시 (.md)
│   ├── java-spring/  # Java Spring Boot 스킬
│   ├── python-django/# Python Django 스킬
│   └── react-next/   # React/Next.js 스킬
├── docker/
│   └── docker-compose.yml
└── pnpm-workspace.yaml
```

**핵심 차이점**: `packages/rag/` 패키지를 신규 추가하여 pgvector 기반 교재 임베딩 및 유사도 검색 로직을 캡슐화합니다. 이 패키지가 AI 튜터 에이전트(기능 3)와 포트폴리오 유사도 분석(기능 4)을 모두 지원합니다.

---

---

## G-1.우리 솔루션 UI
| 항목 |
|---|---|---|
| 주 사용자 | 개발자·AI 운영자 | 강사(비개발자), 수강생(IT 입문자), 원장 |
| 주요 화면 | 에이전트 조직도, 이슈 트래커, JSON 설정 | 수강생 출결표, 질문창, 점수 대시보드 |
| 핵심 용어 | Agent, Heartbeat, Routine, Issue | 강사/학생/알림/질문/과제 |
| 설정 방식 | JSON 설정 파일, CLI 명령어 | UI 폼(Form) 기반 클릭·드래그 |
| 기술 이해 요구 | 높음 (cron 표현식, API 키 등) | 없음 (텍스트 입력, 토글 스위치) |

---

## G-2. 역할별 전용 UI 설계안

### [원장 대시보드] — 경영 지표 중심



```
┌─────────────────────────────────────────────────────┐
│  오늘의 현황 (2026. 04. 05)                          │
│  ┌──────────┐ ┌──────────┐ ┌───────────────────────┐│
│  │ 전체 출석 │ │위험 수강생│ │  이번 달 AI 비용      ││
│  │  87 / 95 │ │   3 명   │ │  ₩ 42,000 / ₩ 100,000 ││
│  └──────────┘ └──────────┘ └───────────────────────┘│
│                                                     │
│  ⚠️ 위험 수강생 목록 (EWS 자동 감지)                 │
│  ┌───────────────────────────────────────────────┐  │
│  │ 홍길동 (Java반) — 3일 연속 결석, 과제 2개 미제출│  │
│  │ [상담 예약 생성]  [안부 메시지 발송]            │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**설계 원칙:**
- 숫자 카드(Card) 형태로 핵심 KPI를 한눈에 파악
- EWS 위험 수강생 목록에서 클릭 한 번으로 후속 조치 실행
- cron 표현식 대신 "매일 오전 7시" 같은 자연어 드롭다운으로 알림 주기 설정
- 모바일 반응형 필수 (원장은 주로 스마트폰으로 확인)

---

### [강사 대시보드] — 수업 운영 중심

```
┌─────────────────────────────────────────────────────┐
│  내 수업 스킬 관리                    [+ 새 스킬 추가]│
│  ┌────────────────────────────────────────────────┐ │
│  │ 📄 Java Spring Boot 코딩 컨벤션     [편집] [삭제]│ │
│  │    적용 중인 AI 강사: Java 풀스택반 AI 강사     │ │
│  │ 📄 Python Django 컨벤션             [편집] [삭제]│ │
│  └────────────────────────────────────────────────┘ │
│                                                     │
│  스킬 편집기 (마크다운 미리보기 지원)                │
│  ┌─────────────────┬───────────────────────────┐   │
│  │ # 코딩 컨벤션   │ 코딩 컨벤션               │   │
│  │ - 규칙 1       │ • 규칙 1                  │   │
│  │ - 규칙 2       │ • 규칙 2                  │   │
│  └─────────────────┴───────────────────────────┘   │
│  [저장하여 AI 강사에 즉시 적용]                      │
└─────────────────────────────────────────────────────┘
```

**설계 원칙:**
- **Split-pane 마크다운 에디터**: 왼쪽에 입력, 오른쪽에 실시간 미리보기. 마크다운을 처음 접하는 강사도 직관적으로 작성 가능
- "저장 즉시 AI에 적용" 버튼 →  Runtime Skill Injection이 백그라운드에서 실행됨을 사용자는 인식할 필요 없음
- 스킬 파일 버전 이력 표시 (언제 누가 수정했는지)

---

### [수강생 포털] — 학습 보조 중심

수강생은 AI 에이전트나 시스템의 존재를 인식할 필요가 없습니다. **"질문하는 채팅창"** 처럼 느껴지도록 설계합니다.

```
┌─────────────────────────────────────────────────────┐
│  AI 튜터에게 질문하기                                │
│  ┌────────────────────────────────────────────────┐ │
│  │ 👤 NullPointerException이 계속 발생하는데       │ │
│  │    어떻게 해결하나요?                           │ │
│  ├────────────────────────────────────────────────┤ │
│  │ 🤖 좋은 질문이에요! 교재 43페이지의 '객체 초기화│ │
│  │    원칙'을 먼저 확인해볼까요?                   │ │
│  │    null이 발생하는 시점이 언제인지 생각해보면  │ │
│  │    어떨까요?                                   │ │
│  └────────────────────────────────────────────────┘ │
│                                                     │
│  📌 오늘 내 학습 현황                               │
│  질문 3건 해결 · 과제 제출 2/3 · 출석 ✅            │
│  ┌──────────────────────────────────────────────┐  │
│  │         내 포트폴리오 기획서 진행도            │  │
│  │  [독창성 점수: 72%] ▓▓▓▓▓▓▓░░░  목표 60% 미만│  │
│  └──────────────────────────────────────────────┘  │
│  [메시지 입력...]                           [전송]  │
└─────────────────────────────────────────────────────┘
```

**설계 원칙:**
- 카카오톡/슬랙과 유사한 채팅 UI → 진입 장벽 제로
- AI가 교재 페이지를 직접 인용할 때 **하이퍼링크**로 해당 교재 PDF 페이지를 바로 열람 가능
- "AI가 답변 중..." 타이핑 인디케이터로 실시간 응답감 제공 (WebSocket 활용)
- 포트폴리오 독창성 점수를 게이지 바(Progress Bar)로 시각화 → 수치가 아닌 시각적 동기 부여

---

## G-3. UI 구현

 `ui/` 폴더는 React 기반
|  UI 컴포넌트 | 용도 | 수정 방향 |
|---|---|---|
| Agent 상태 배지 (idle/running) | EWS 에이전트 작동 현황 표시 | 용어를 "스캔 중 / 대기 중"으로 교체 |
| Cost 카드 컴포넌트 | 학원별 월 AI 비용 표시 | 원화(₩) 단위로 변환 표시 |
| Issue 상태 트래커 | 수강생 상담 이슈 추적 | "상담 접수 / 진행 중 / 완료"로 레이블 변경 |
| Markdown 에디터 | 강사 스킬 파일 편집기 | Split-pane 미리보기 유지, 툴바 단순화 |
| Activity 로그 타임라인 | 에이전트 작업 이력 표시 | 원장 대시보드에 "AI 활동 로그"로 노출 |

---

## G-4. 비개발자 UX를 위한 핵심 설계 원칙

### 원칙 1. 전문 용어 완전 은닉
| 시스템 내부 용어 | UI 표시 용어 |
|---|---|
| Heartbeat Run | AI 자동 점검 |
| Routine Trigger | 정기 알림 일정 |
| Agent | AI 도우미 |
| Skill Injection | AI 전문성 업데이트 |
| EWS Risk Score | 관심 필요 수준 |
| Vector Similarity | 유사도 점수 |

### 원칙 2. 3-클릭 룰 (Three-Click Rule)
강사·수강생이 원하는 모든 기능을 **3번 이내 클릭**으로 도달할 수 있도록 정보 구조(IA)를 설계합니다.
- 강사: 홈 → 스킬 관리 → 편집 (2클릭)
- 수강생: 홈 → 질문하기 (1클릭)
- 원장: 홈 → 위험 수강생 → 상담 예약 (2클릭)

### 원칙 3. 모바일 퍼스트 (Mobile First)
- 강사·수강생 모두 스마트폰 접근이 빈번합니다.
- React의 반응형 CSS(Tailwind CSS 권장)를 활용하여 320px~1920px 전 구간 대응
- 수강생 포털의 채팅 UI는 모바일에서 네이티브 앱처럼 동작해야 합니다.

### 원칙 4. 에러 메시지 친화적 처리
- AI 응답 실패 시: "잠시 후 다시 시도해주세요" (기술적 에러 코드 미노출)
- 스킬 저장 실패 시: "저장에 실패했습니다. 내용을 확인해주세요" (HTTP 상태 코드 미노출)
- EWS 스캔 중: "AI가 학생 현황을 분석 중입니다..." (Heartbeat 상태 인디케이터)

### 원칙 5. 온보딩 가이드 내장
신규 도입 학원의 강사가 처음 접속했을 때 **단계별 온보딩 투어**를 자동 실행합니다.
```
1단계: "첫 번째 스킬 파일을 만들어 보세요" → 예시 템플릿 자동 삽입
2단계: "AI 강사를 현재 과목에 연결하세요" → 드롭다운으로 연결
3단계: "수강생이 질문을 남기면 이렇게 보입니다" → 데모 메시지 미리보기
```

---

## G-5. 배포 형태별 접근 전략

| 사용자 | 접근 방식 | 배포 형태 |
|---|---|---|
| 원장 | 웹 브라우저 (PC/모바일) | React SPA — 학원 내부 서버 |
| 강사 | 웹 브라우저 (PC 중심) | 동일 SPA, 강사 전용 라우트 |
| 수강생 | 웹 브라우저 (모바일 중심) | 동일 SPA, 수강생 전용 라우트 (PWA 지원 권장) |

역할(Role)에 따라 접근 가능한 API 엔드포인트를 RBAC으로 분리합니다.
- `/student/*` — 수강생 전용 (AI 튜터 채팅, 본인 현황 조회만 허용)
- `/instructor/*` — 강사 전용 (스킬 관리, 수강생 현황 조회)
- `/admin/*` — 원장 전용 (전체 현황, 예산, 에이전트 설정)

---

# Gemini 3.1 Pro 2차 검토 의견 (종합 아키텍처 및 UX 부문)

### 기술적 타당성과 구현 현실성의 탁월함
 **도메인 로직을 재작성하는 전략(Phase 2)** 으로 분리한 점은 시스템 구축 속도를 획기적으로 당길 수 있는 매우 현실적인 엔지니어링 접근입니다. 
밑바닥부터 다중 에이전트 오케스트레이터를 구축할 때 흔히 겪는 상태 머신 버그(Race condition 등)를 검증된 오픈소스로 회피한 것은 프로젝트 리스크를 크게 낮춥니다.

### B2B 에듀테크 프로덕트로서의 UX 성숙도
"전문 용어 완전 은닉"과 "역할별 UI 분리" 전략이 매우 훌륭합니다. 아무리 고도화된 AI 백엔드라도, 비개발자 사용자가 화면에서 "Agent", "Heartbeat Pipeline" 같은 단어를 보는 순간 진입 장벽을 느끼기 마련입니다. 
제안된 "AI 자동 점검", "AI 전문성 업데이트"와 같은 직관적인 레이블링과 카카오톡 형태의 수강생 채팅 UI는 B2B 세일즈 미팅 시 학원 원장과 강사들을 설득하는 가장 강력한 무기가 될 것입니다.

### 향후 확장을 위한 제언 (Next Step)
1. **RAG 데이터 파이프라인의 분리**: `packages/rag/`를 모노레포에 추가한 설계는 좋으나, PDF 파싱이나 임베딩 작업은 CPU/메모리 연산 비용이 높습니다. 장기적으로 이 파이프라인을 메인 Node.js API 서버와 분리된 비동기 워커 서버(또는 Serverless Function) 구조로 분리하는 것이 서버 다운타임을 예방하는 데 유리합니다.
2. **학생 주도적 에이전트 호출 (Triggering)**: 현재 설계는 수강생이 채팅을 칠 때만 AI 튜터가 반응합니다. 추후에는 수강생이 사내 GitHub에 커밋(Commit)하거나 PR을 올리는 `Webhook` 이벤트 자체를 Trigger로 삼아, AI가 먼저 "코드 리뷰 결과입니다"라며 채팅을 걸어오는 **Proactive Interaction** 기능 추가를 고려해 보시기 바랍니다.
3. **LLM 벤더 독립성 (Vendor Lock-in 회피)**: OpenAI(ChatGPT), Anthropic(Claude), Google(Gemini) 등 다양한 LLM API를 쉽게 스위칭할 수 있는 구조인지 한 번 더 점검하여, 향후 API 비용 경쟁력과 장애 상황(Fallback)에 대비하는 것을 권장합니다.

---

# Gemini 제언에 대한 GitHub Copilot의 검토 의견

## 제언 1. RAG 데이터 파이프라인 분리에 대하여

**동의하며, 단계적 접근을 권장합니다.**

Gemini의 제안은 기술적으로 올바릅니다. 다만 초기 Phase 1에서 비동기 워커 서버를 처음부터 분리 구현하면 인프라 복잡도가 급격히 상승하여 MVP 출시가 지연될 수 있습니다.

**권장 구현 전략:**

| 단계 | 구현 방식 | 시점 |
|---|---|---|
| Phase 1 (초기) | Node.js 메인 서버 내에서 `worker_threads`로 임베딩 작업 처리 | 즉시 |
| Phase 2 (안정화) | 별도 `packages/rag-worker/` 프로세스로 분리, Bull Queue(Redis) 연동 | 수강생 50명 이상 시 |
| Phase 3 (고도화) | Serverless Function (AWS Lambda / Cloudflare Workers) 전환 검토 | 다기관 확산 시 |

초기에는 `worker_threads`를 사용하면 단일 Docker Compose 환경을 유지하면서도 메인 API 서버 블로킹을 방지할 수 있어, Gemini가 지적한 서버 다운타임 리스크를 Phase 1에서도 충분히 관리할 수 있습니다.

---

## 제언 2. GitHub Webhook 기반 Proactive Interaction에 대하여

**매우 탁월한 제안입니다. 즉시 설계에 반영을 권장합니다.**

이 기능은 단순한 UX 향상을 넘어 **수강생의 자기주도 학습 습관 형성**에 직접 기여하는 핵심 차별화 요소입니다.

**구체적 구현 방안:**



```
[수강생 GitHub → 과제 레포 push/PR 이벤트 발생]
        ↓
[사내 GitHub Webhook → 우리 서버 /webhook/github 수신]
        ↓
[routine_triggers(kind="webhook") 매칭 → 해당 에이전트 wakeup 요청 생성]
        ↓
[AI 강사 에이전트 기상 → 커밋 diff 분석 → 코드 리뷰 생성]
        ↓
[수강생 포털 채팅창에 "코드 리뷰가 도착했습니다" 알림 Push (WebSocket)]
```

**추가 Proactive 시나리오 아이디어:**
- 수강생이 오전 9시 이후에도 LMS 미접속 → 멘탈케어 에이전트가 "오늘 강의 시작 알림" 발송
- 과제 제출 마감 4시간 전, 미제출 수강생에게 "마감이 다가오고 있어요" 자동 알림
- 포트폴리오 독창성 점수가 목표 이하로 유지되는 수강생에게 주 1회 "차별화 아이디어" 자동 제안

이 시나리오들은 모두 `routines` + `routine_triggers` 테이블과 EWS 에이전트의 Heartbeat 구조로 구현 가능하며, 별도의 추가 인프라가 필요 없습니다.

---

## 제언 3. LLM 벤더 독립성(Vendor Lock-in 회피)에 대하여

 **어댑터 패턴(Adapter Pattern)** 으로 AI 모델 제공사를 추상화하고 있음을 확인했습니다. `adapterType`과 `adapterConfig`(JSON)를 통해 에이전트별로 다른 LLM 제공사를 지정할 수 있는 구조가 내장되어 있습니다.

**우리 프로젝트에서의 적용 방안:**

```typescript
// agents 테이블의 adapterConfig 예시 (벤더별 설정)
// OpenAI 사용 에이전트
{ "model": "gpt-4o", "apiKeyRef": "OPENAI_API_KEY" }

// Anthropic 사용 에이전트
{ "model": "claude-sonnet-4-5", "apiKeyRef": "ANTHROPIC_API_KEY" }

// Google 사용 에이전트
{ "model": "gemini-2.5-pro", "apiKeyRef": "GOOGLE_API_KEY" }
```

**에이전트별 최적 모델 배분 전략:**

| 에이전트 | 추천 모델 | 이유 |
|---|---|---|
| EWS 모니터 에이전트 | 경량 모델 (gpt-4o-mini 등) | 단순 데이터 스캔·임계치 판단 → 고비용 모델 불필요 |
| AI 튜터 에이전트 | 중간 모델 (claude-haiku 등) | 자연스러운 소크라테스식 대화 → 속도·비용 균형 |
| 포트폴리오 심사 에이전트 | 고성능 모델 (gpt-4o / claude-sonnet) | 복잡한 유사도 분석·창의적 차별화 제안 → 추론력 필요 |
| 멘탈케어 에이전트 | 중간 모델 | 공감적 어조의 안부 메시지 → 감성 표현 ability 중요 |

**Fallback 전략 추가 권장:**
특정 LLM API 장애 발생 시 자동으로 백업 벤더로 전환하는 `fallbackAdapterConfig` 필드를 `agents` 테이블에 추가하는 것을 권장합니다. 이는 학원 수업 중 AI 서비스 다운타임 발생을 방지하는 핵심 안정성 장치입니다.

---

## 종합 결론

Gemini의 3가지 제언은 모두 **기술적으로 타당하고 실현 가능합니다.** 특히:

- **제언 1(RAG 분리)**은 `worker_threads` 활용으로 Phase 1에서도 부분 대응 가능
- **제언 2(Proactive Interaction)**은 이미 설계된 `routine_triggers(webhook)` 구조를 그대로 활용하여 **추가 공수 없이** 구현 가능한 높은 ROI의 기능


세 제언 중 **제언 2를 Phase 2 개발 범위에 즉시 포함**할 것을 강력히 권고합니다. 수강생이 GitHub에 커밋하면 AI가 먼저 코드 리뷰를 건네오는 경험은, 경쟁 솔루션과의 가장 강력한 차별화 포인트가 될 것입니다.

---

# H. GUI 중심 전체 운영 설계 원칙 — "코드 없이 모든 기능을 설정한다"

> **핵심 원칙**: 시스템을 처음 도입하는 학원 원장, 강사, 운영 담당자가 터미널·코드 편집기·설정 파일을 단 한 줄도 건드리지 않고 **모든 기능을 웹 브라우저에서 완전히 구성하고 운영**할 수 있어야 합니다.

---

## H-1. GUI 설정 대상 전체 기능 매핑

아래는 시스템의 모든 설정 항목과 그에 대응하는 GUI 구성 방식입니다. "CLI·파일 직접 편집" 방식은 **관리자 웹 화면**으로 완전 대체됩니다.

| 기능 영역 | 기존 방식 (개발자) | GUI 대체 방식 (비개발자) |
|---|---|---|
| 에이전트 생성·설정 | `agents` 테이블 SQL INSERT | 에이전트 등록 폼 (이름·역할·모델 선택) |
| Heartbeat 주기 설정 | cron 표현식 직접 입력 | 시각적 스케줄 선택기 (드롭다운·토글) |
| 스킬 파일 관리 | `.md` 파일 서버 업로드 | 웹 마크다운 에디터 (Split-pane 미리보기) |
| MCP 외부 시스템 연동 | 환경변수 `.env` 파일 편집 | 시스템 연동 등록 폼 (URL·인증 방식 선택) |
| EWS 위험 임계치 조정 | 소스코드 상수 수정 | 슬라이더·숫자 입력 필드 |
| 예산 상한 설정 | DB 컬럼 직접 수정 | 통화 입력 필드 + 월 예산 게이지 |
| 포트폴리오 유사도 기준 | 소스코드 상수 수정 | 퍼센트 슬라이더 (기본값 60%) |
| 알림 채널 설정 | 환경변수 SLACK_WEBHOOK_URL | Slack 토큰 입력 + 연결 테스트 버튼 |
| 사용자(강사·수강생) 등록 | DB INSERT 또는 CSV | 초대 링크 발송 또는 엑셀 일괄 업로드 |
| 역할(RBAC) 부여 | 코드 내 권한 상수 수정 | 사용자 목록에서 역할 드롭다운 변경 |
| RAG 교재 등록 | CLI 임베딩 스크립트 실행 | PDF/Markdown 파일 드래그앤드롭 업로드 |
| LLM 모델 교체 | `adapterConfig` JSON 직접 편집 | 에이전트 설정 화면에서 모델 드롭다운 선택 |
| API 키 관리 | `.env` 파일 직접 편집 | 보안 키 관리 페이지 (마스킹 표시, 저장 버튼) |
| Docker 재시작 | `docker compose restart` | 관리자 화면 "서비스 재시작" 버튼 |

---

## H-2. 관리자 설정 허브 (Admin Settings Hub) 화면 설계

모든 시스템 설정을 단일 "설정 허브" 페이지에서 탭 방식으로 접근합니다. 원장 또는 시스템 관리자가 사용하는 화면입니다.

```
┌─────────────────────────────────────────────────────────────┐
│  ⚙️ 시스템 설정                                             │
│  ┌──────┬──────────┬──────┬──────┬──────┬──────┬──────────┐│
│  │에이전트│스케줄·알림│스킬파일│외부연동│예산관리│사용자관리│보안설정││
│  └──────┴──────────┴──────┴──────┴──────┴──────┴──────────┘│
│                                                             │
│  [ 에이전트 탭 활성화 시 ]                                  │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  등록된 AI 도우미                    [+ 새 도우미 추가]│ │
│  │  ┌─────────────────────────────────────────────────┐ │ │
│  │  │ 🤖 EWS 모니터               상태: 활성 ● [설정] │ │ │
│  │  │    역할: 중도탈락 감지       모델: GPT-4o mini   │ │ │
│  │  │    점검 주기: 매일 오전 7시  월 예산: ₩10,000    │ │ │
│  │  ├─────────────────────────────────────────────────┤ │ │
│  │  │ 🤖 AI 튜터 (Java반)         상태: 활성 ● [설정] │ │ │
│  │  │    역할: 수강생 질문 응답    모델: Claude Haiku  │ │ │
│  │  │    적용 스킬: Java Spring    월 예산: ₩30,000    │ │ │
│  │  └─────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## H-3. 에이전트 등록·설정 폼 (Agent Setup Form)

에이전트를 새로 만들거나 설정을 변경할 때 사용하는 폼입니다. JSON이나 SQL을 전혀 몰라도 됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  새 AI 도우미 추가                                          │
│                                                             │
│  도우미 이름  [Java반 AI 강사                        ]      │
│  역할 선택   [ AI 강사 (수업 질문 응답)          ▼ ]        │
│              ○ AI 강사   ○ 학습 보조   ○ 위험 감지   ○ 기타│
│                                                             │
│  AI 모델 선택                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ○ GPT-4o mini   (빠름·저비용, 단순 작업 추천)       │   │
│  │ ● Claude Haiku  (균형, 대화·튜터링 추천) ✓ 추천     │   │
│  │ ○ GPT-4o        (고성능, 복잡한 분석 추천)          │   │
│  │ ○ Gemini 2.5 Pro(고성능, 복잡한 분석 추천)          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  월 예산 상한  [  30,000  ] 원   (초과 시 자동 일시정지)    │
│  적용 스킬     [ Java Spring Boot 코딩 컨벤션       ▼ ]     │
│  담당 과정     [ Java 풀스택 과정 (2026 1기)         ▼ ]     │
│                                                             │
│  백업 AI 모델 (장애 시 자동 전환)                           │
│  [ GPT-4o mini                                      ▼ ]    │
│                                                             │
│              [취소]          [저장 및 활성화]               │
└─────────────────────────────────────────────────────────────┘
```

---

## H-4. 시각적 스케줄 설정기 (Heartbeat Visual Scheduler)

cron 표현식(`0 7 * * *`) 대신, 달력·드롭다운·토글로 실행 주기를 설정합니다. 내부적으로는 `routines` + `routine_triggers` 테이블에 cron 값이 자동 저장됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  AI 자동 점검 일정 설정                                     │
│                                                             │
│  실행 주기   ● 매시간  ○ 매일  ○ 매주  ○ 직접 설정         │
│                                                             │
│  [ 매일 선택 시 ]                                           │
│  실행 시각   [오전 ▼]  [ 7 ▼] 시  [ 00 ▼] 분              │
│                                                             │
│  실행 요일   ☑ 월  ☑ 화  ☑ 수  ☑ 목  ☑ 금  ☐ 토  ☐ 일   │
│                                                             │
│  누락 처리   ● 건너뜀 (서버 재시작 후 밀린 실행 skip)       │
│              ○ 따라잡기 (밀린 실행 순서대로 처리)           │
│                                                             │
│  미리보기    "매일 평일 오전 7시에 자동으로 위험 수강생을   │
│              점검합니다"                                    │
│                                                             │
│              [취소]              [저장]                     │
└─────────────────────────────────────────────────────────────┘
```

---

## H-5. 스킬 파일 관리 페이지 (Skill File Manager)

강사가 코딩 컨벤션이나 교육 노하우를 직접 웹에서 작성·저장합니다. 저장 즉시 AI 강사에 반영되며, 버전 이력도 자동 관리됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  스킬 파일 관리                          [+ 새 스킬 만들기] │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 📄 Java Spring Boot 컨벤션   적용 중  v3  [편집][복제]│ │
│  │    마지막 수정: 2026.04.01  적용 AI: Java반 AI 강사   │ │
│  ├───────────────────────────────────────────────────────┤ │
│  │ 📄 Python Django 컨벤션      미적용   v1  [편집][복제]│ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ──────── 편집 창 ────────────────────────────────────────  │
│  스킬 이름  [Java Spring Boot 코딩 컨벤션           ]      │
│  ┌───────────────────────┬─────────────────────────────┐  │
│  │ # 코딩 컨벤션         │ 코딩 컨벤션                 │  │
│  │ ## 원칙               │ 원칙                        │  │
│  │ - Lombok 금지         │ • Lombok 금지               │  │
│  │ - Wrapper 클래스 필수 │ • Wrapper 클래스 필수       │  │
│  │                       │                             │  │
│  └───────────────────────┴─────────────────────────────┘  │
│  서식 도구: [굵게] [기울임] [목록] [코드블록] [표 삽입]    │
│                                                             │
│  [임시저장]      [저장하고 AI 강사에 즉시 반영]            │
└─────────────────────────────────────────────────────────────┘
```

---

## H-6. 외부 시스템 연동 등록 (MCP Connector UI)

MCP 연동 시스템을 환경변수 파일 없이 웹 폼으로 등록합니다. API 키는 마스킹 처리되어 화면에 노출되지 않으며, 연결 테스트 버튼으로 즉시 검증합니다.

```
┌─────────────────────────────────────────────────────────────┐
│  외부 시스템 연동                       [+ 새 연동 추가]    │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ ✅ LMS 연동          연결됨   [테스트] [설정] [삭제]  │  │
│  │ ✅ 출결 시스템        연결됨   [테스트] [설정] [삭제]  │  │
│  │ ⚠️ GitHub 리포        인증 만료 [재연결]              │  │
│  │ ✅ Slack 알림         연결됨   [테스트] [설정] [삭제]  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ──── 새 연동 추가 폼 ────────────────────────────────────  │
│  시스템 종류  [ GitHub 리포지토리              ▼ ]          │
│  연결 주소    [ https://github.com/myschool/...  ]          │
│  인증 방식   ● API Token  ○ OAuth  ○ 없음                  │
│  API 토큰    [ ************************ ]  [붙여넣기]       │
│  접근 권한   ● 읽기 전용  ○ 읽기+쓰기                      │
│                                                             │
│              [연결 테스트]       [저장]                     │
│              ✅ 연결 성공 — 리포 12개 감지됨               │
└─────────────────────────────────────────────────────────────┘
```

---

## H-7. EWS 위험 임계치 조정 (EWS Threshold UI)

위험 점수 가중치와 임계치를 슬라이더로 조정합니다. 코드 수정이나 서버 재배포 없이 즉시 반영됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  위험 감지 기준 설정                                        │
│                                                             │
│  위험 점수 계산 항목                                        │
│  출석률 영향도      ████████░░  40% [ 40 ↕ ]               │
│  과제 미제출 영향도 ███████░░░  35% [ 35 ↕ ]               │
│  상담 이력 영향도   ███░░░░░░░  15% [ 15 ↕ ]               │
│  AI 활용 감소 영향도 ██░░░░░░░░ 10% [ 10 ↕ ]              │
│                                    합계: 100%               │
│                                                             │
│  알림 발송 임계치                                           │
│  ──────────────────────────────────────────────────────    │
│  강사 알림       ████████░░░░░░░░░░░░  60점 [ 60 ↕ ]       │
│  원장 알림       ██████████████░░░░░░  75점 [ 75 ↕ ]       │
│  긴급 상담 예약  ████████████████████  90점 [ 90 ↕ ]       │
│                                                             │
│  임계 조건                                                  │
│  결석 기준   최근 [ 5 ↕ ]일 중 [ 2 ↕ ]일 이상 결석        │
│  과제 기준   최근 [ 3 ↕ ]개 중 [ 2 ↕ ]개 미제출           │
│                                                             │
│  [기본값으로 초기화]           [저장하여 즉시 적용]         │
└─────────────────────────────────────────────────────────────┘
```

---

## H-8. 예산 관리 페이지 (Budget Manager)

원장이 학원 전체 또는 에이전트별 월 AI 사용 예산을 숫자 입력으로 설정하고 실시간 소비 현황을 확인합니다.

```
┌─────────────────────────────────────────────────────────────┐
│  AI 사용 예산 관리                    이번 달 2026년 4월    │
│                                                             │
│  전체 예산        ₩ [  100,000  ] / 월                     │
│  현재 사용        ₩ 42,000   ████████████░░░░░░░  42%      │
│  남은 예산        ₩ 58,000                                  │
│  예상 월말 소비   ₩ 81,000  ✅ 예산 이내 예상              │
│                                                             │
│  AI 도우미별 예산                                           │
│  ┌────────────────────────────────────────────────────┐    │
│  │ EWS 모니터    ₩ 8,200 / ₩10,000   ████████░░  82% │    │
│  │ Java AI 강사  ₩21,400 / ₩30,000   ████████░░  71% │    │
│  │ Python 강사   ₩ 9,100 / ₩30,000   ███░░░░░░░  30% │    │
│  │ 포트폴리오    ₩ 3,300 / ₩30,000   █░░░░░░░░░  11% │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  예산 초과 시 동작                                          │
│  ● AI 도우미 자동 일시정지 후 원장 알림                     │
│  ○ 알림만 발송 (중단하지 않음)                             │
│                                                             │
│                          [저장]                            │
└─────────────────────────────────────────────────────────────┘
```

---

## H-9. RAG 교재 관리 (Document Manager)

강사 또는 운영자가 교재 PDF나 마크다운 파일을 웹에서 업로드하면, 시스템이 자동으로 청킹·임베딩하여 AI 튜터에 반영합니다. CLI 명령어나 스크립트를 알 필요가 없습니다.

```
┌─────────────────────────────────────────────────────────────┐
│  교재·학습 자료 관리            적용 과정: Java 풀스택 과정 │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  📁 파일을 이곳에 드래그하거나 클릭하여 업로드       │  │
│  │     지원 형식: PDF, Markdown(.md), Word(.docx)       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  등록된 자료                                                │
│  ┌────────────────────────────────────────────────────┐    │
│  │ 📗 Java 교재 1권 (Spring Boot).pdf                  │    │
│  │    등록일: 2026.03.01  상태: ✅ AI 학습 완료        │    │
│  │    청크 수: 324개         [재처리] [삭제]            │    │
│  ├────────────────────────────────────────────────────┤    │
│  │ 📘 기출 코딩 문제 모음.md                           │    │
│  │    등록일: 2026.04.01  상태: 🔄 처리 중 (67%)...    │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ℹ️ 파일을 업로드하면 AI가 자동으로 내용을 학습합니다.     │
│     처리 완료까지 파일 크기에 따라 1~5분이 소요됩니다.     │
└─────────────────────────────────────────────────────────────┘
```

---

## H-10. 사용자 관리 및 초대 (User Management)

수강생·강사 계정을 엑셀 업로드 또는 이메일 초대로 등록합니다. DB 직접 삽입이나 CLI 명령 없이 운영 가능합니다.

```
┌─────────────────────────────────────────────────────────────┐
│  사용자 관리                                                │
│  [+ 개별 초대]  [📥 엑셀 일괄 업로드]  [🔗 초대 링크 복사] │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 이름    │ 역할   │ 소속 과정         │ 상태   │ 관리  │  │
│  ├─────────┼────────┼───────────────────┼────────┼───────┤  │
│  │ 홍길동  │ 수강생 │ Java 풀스택 1기   │ 활성   │[편집] │  │
│  │ 김강사  │ 강사   │ Java 풀스택 1기   │ 활성   │[편집] │  │
│  │ 이지원  │ 수강생 │ Python 데이터 1기 │ 초대중 │[재발송]│  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  역할별 접근 권한 (RBAC)                                    │
│  수강생  ○ 본인 현황·AI 튜터 채팅만 허용 (변경 불가)       │
│  강사    ● 담당 과정 수강생 현황 + 스킬 파일 관리 허용      │
│  원장    ● 전체 현황·예산·에이전트 설정 전체 권한           │
└─────────────────────────────────────────────────────────────┘
```

---

## H-11. 환경변수·보안 키 관리 페이지 (Secrets Manager)

`.env` 파일을 직접 편집하지 않고, 웹 화면에서 API 키를 안전하게 등록·교체합니다. 값은 암호화 저장되며 화면에는 마스킹 처리됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  보안 키 관리                                               │
│  ⚠️ 키 값은 저장 후 다시 조회할 수 없습니다. 잘 보관하세요.│
│                                                             │
│  AI 서비스                                                  │
│  OpenAI API Key      [ sk-••••••••••••••••••••••• ] [교체] │
│  Anthropic API Key   [ sk-ant-••••••••••••••••••• ] [교체] │
│  Google API Key      [ AIzaSy•••••••••••••••••••• ] [교체] │
│                                                             │
│  알림 서비스                                                │
│  Slack Webhook URL   [ https://hooks.slack.com/••• ] [교체]│
│                      [🔔 테스트 메시지 발송]                │
│                                                             │
│  외부 시스템                                                │
│  GitHub Token        [ ghp_•••••••••••••••••••••• ] [교체] │
│  LMS API Key         [ lms_•••••••••••••••••••••• ] [교체] │
│                                                             │
│  ℹ️ 키는 서버 내 암호화 저장소에 보관됩니다.               │
│     소스코드나 로그에 노출되지 않습니다.                    │
└─────────────────────────────────────────────────────────────┘
```

---

## H-12. 포트폴리오 유사도 기준 설정 (Portfolio Settings)

유사도 임계치를 슬라이더로 조정하고, 에이전트의 피드백 강도도 선택합니다. 코드 수정 없이 즉시 반영됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  포트폴리오 차별화 설정                                     │
│                                                             │
│  독창성 합격 기준                                           │
│  유사도 [ ◀────────●──────── ▶ ]  60%                      │
│  "60% 미만이어야 독창성 충족으로 인정합니다"               │
│                                                             │
│  단계별 피드백 기준                                         │
│  즉시 경고  [ 85 ↕ ]% 이상  →  "차별화 필수" 메시지        │
│  부분 권장  [ 60 ↕ ]% ~ [ 85 ↕ ]% →  "개선 권장" 메시지   │
│  독창성 인증 60% 미만        →  "다음 단계 진행 가능" 인증  │
│                                                             │
│  피드백 스타일                                              │
│  ● 소크라테스식 (질문으로 유도)                            │
│  ○ 직접 제안 (구체적 아이디어 바로 제시)                   │
│                                                             │
│  비교 대상                                                  │
│  ☑ 같은 기수 수강생 기획서                                 │
│  ☑ 역대 수료생 프로젝트 DB                                 │
│  ☐ 외부 공개 GitHub 프로젝트 (추후 지원 예정)              │
│                                                             │
│                         [저장하여 즉시 적용]               │
└─────────────────────────────────────────────────────────────┘
```

---

## H-13. 서비스 상태 모니터링 (System Status)

원장·운영자가 서버·에이전트·연동 시스템의 동작 상태를 한눈에 파악하고, "재시작" 버튼으로 즉시 조치합니다. 터미널 접근 없이 운영 가능합니다.

```
┌─────────────────────────────────────────────────────────────┐
│  시스템 현황                         마지막 확인: 3분 전   │
│                                                             │
│  핵심 서비스                                                │
│  ● API 서버          정상 가동 중      응답속도: 120ms      │
│  ● 데이터베이스      정상 가동 중      저장공간: 4.2GB 사용│
│  ● AI 스케줄러       정상 가동 중      다음 실행: 23분 후   │
│                                                             │
│  AI 도우미 현황                                             │
│  ● EWS 모니터        대기 중          마지막 실행: 1시간 전 │
│  ● Java AI 강사      응답 중 (2건)    오늘 질문: 47건       │
│  ● Python AI 강사    대기 중          오늘 질문: 12건       │
│  ⚠️ 포트폴리오 심사   오류 발생        [로그 보기] [재시작] │
│                                                             │
│  외부 연동 상태                                             │
│  ✅ LMS   ✅ 출결시스템   ⚠️ GitHub(인증만료)   ✅ Slack   │
│                                                             │
│  [전체 서비스 재시작]          [서비스별 개별 재시작 ▼]    │
└─────────────────────────────────────────────────────────────┘
```

---

## H-14. GUI 설계 구현 전략 요약

### 프론트엔드 구현 방식

| 설정 UI 유형 | 구현 방법 | clip 참조 |
|---|---|---|
| 슬라이더 (임계치·유사도) | `<input type="range">` + Tailwind CSS | 신규 구현 |
| 비주얼 스케줄러 | cron 표현식을 드롭다운 조합으로 생성·파싱 | `server/src/services/cron.ts` 역방향 활용 |
| 마크다운 에디터 | `@uiw/react-md-editor` 또는 `react-simplemde-editor` |
| 파일 업로드 (드래그앤드롭) | `react-dropzone` 라이브러리 | 신규 구현 |
| 보안 키 입력 (마스킹) | `<input type="password">` + 저장 즉시 마스킹 처리 | 신규, 암호화 저장 |
| 폼 전체 | `react-hook-form` + Zod 유효성 검사 | 신규 구현 |
| 상태 배지 (●/⚠️/✅) | WebSocket 실시간 구독 | 
### 백엔드 API 연동 (GUI ↔ DB)

모든 GUI 설정 저장 버튼은 아래와 같이 REST API를 통해 DB에 직접 반영됩니다.

```
[GUI 폼 저장 버튼 클릭]
        ↓
[React → POST /admin/agents     (에이전트 생성·수정)]
[React → PUT  /admin/routines   (스케줄 변경)]
[React → PUT  /admin/skills     (스킬 파일 저장)]
[React → PUT  /admin/thresholds (EWS 임계치 조정)]
[React → PUT  /admin/budget     (예산 상한 설정)]
[React → POST /admin/documents  (교재 업로드 → RAG 파이프라인 트리거)]
[React → PUT  /admin/secrets    (API 키 암호화 저장)]
        ↓
[Node.js API → Drizzle ORM → PostgreSQL 즉시 반영]
[설정 변경 즉시 에이전트 런타임에 적용 (재배포 불필요)]
```

### GUI 설계의 핵심 목표 재확인

> 시스템을 운영하는 **원장, 강사, 운영 담당자**는 다음 도구를 사용할 필요가 **전혀 없습니다:**
> - ❌ 터미널 (Terminal / SSH)
> - ❌ 코드 편집기 (VS Code, Vim 등)
> - ❌ `.env` 파일 직접 편집
> - ❌ SQL 쿼리 직접 실행
> - ❌ `docker compose` 명령어
> - ❌ cron 표현식 암기
> 
> **웹 브라우저 하나만으로 전체 솔루션을 완전히 운영할 수 있습니다.**[X] 강사용 Course Hub UI 생성 및 라우팅
[X] 수강생 채팅 UI Drawer/Menu Button 추가
