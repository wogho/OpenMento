# 기여 가이드

기여에 관심을 가져주셔서 감사합니다!

작은 버그 수정부터 큰 기능 추가까지 모든 기여를 환영합니다.

## PR 승인을 받는 두 가지 경로

### 경로 1: 작고 집중된 변경 (가장 빠르게 머지되는 방법)

- **하나**의 명확한 수정/개선 사항을 선택합니다.
- 최소한의 파일만 수정합니다.
- 변경이 매우 명확하고 검토하기 쉬운지 확인합니다.
- 모든 테스트가 통과하고 CI가 그린 상태인지 확인합니다.
- [PR 템플릿](.github/PULL_REQUEST_TEMPLATE.md)을 사용합니다.

이러한 PR은 깔끔하다면 거의 항상 빠르게 머지됩니다.

### 경로 2: 더 크거나 영향도가 높은 변경

- **먼저** GitHub Discussions → #dev 채널에서 논의합니다.
  - 해결하려는 문제를 설명합니다.
  - 아이디어/접근 방법을 공유합니다.
- 대략적인 합의가 이루어지면 구현합니다.
- PR에 다음을 포함합니다:
  - 이전/이후 스크린샷 (또는 UI/동작 변경이 있으면 짧은 영상)
  - 무엇을, 왜 변경했는지에 대한 명확한 설명
  - 작동 증명 (수동 테스트 메모)
  - 모든 테스트 통과 및 CI 그린 확인
  - [PR 템플릿](.github/PULL_REQUEST_TEMPLATE.md) 완전히 작성

이 경로를 따라가는 PR은 크더라도 **훨씬 더 받아들여질 가능성이 높습니다.**

## PR 요구사항 (모든 PR)

### PR 템플릿 사용

모든 풀 리퀘스트는 [`.github/PULL_REQUEST_TEMPLATE.md`](.github/PULL_REQUEST_TEMPLATE.md)의 템플릿을 따라야 합니다. 템플릿에는 필수 섹션이 포함되어 있습니다: 사고 경로, 변경 사항, 검증, 위험, 체크리스트.

### 테스트 통과 필수

PR이 머지되기 전에 모든 테스트를 통과해야 합니다. 먼저 로컬에서 실행하고 푸시 후 CI가 그린 상태인지 확인합니다.

```sh
pnpm test:run      # 전체 테스트 실행
pnpm typecheck     # 타입 검사
```

## 일반 규칙 (두 경로 모두)

- 명확한 커밋 메시지 작성
- PR 제목과 설명을 의미 있게 작성
- 하나의 PR = 하나의 논리적 변경
- 로컬에서 먼저 테스트 실행
- 친절하게 토론에 임하기

## 좋은 PR 메시지 작성하기

PR 설명은 [PR 템플릿](.github/PULL_REQUEST_TEMPLATE.md)을 따라야 합니다. 상단의 "사고 경로"는 프로젝트 최상위에서 수정한 내용까지 이어지는 추론 과정을 설명합니다.

### 사고 경로 예시 1:

> - OpenMento은 AI 에이전트를 통해 교육 기관 운영을 자동화합니다.
> - 수강생들의 포트폴리오 제출물 표절을 탐지하는 기능이 있습니다.
> - 하지만 현재 유사도 임계값이 기관별로 설정 불가능합니다.
> - 따라서 기관별로 임계값을 커스터마이즈할 수 있도록 설정을 추가해야 합니다.
> - 이 PR은 `institution_settings` 테이블에 `similarity_threshold` 컬럼을 추가합니다.
> - 덕분에 각 교육 기관이 자신들의 엄격도에 맞게 표절 탐지 민감도를 조정할 수 있습니다.

### 사고 경로 예시 2:

> - OpenMento은 에이전트 하트비트를 통해 정기 업무를 자동화합니다.
> - 관리자가 에이전트의 실시간 상태를 모니터링할 수 있어야 합니다.
> - 현재 대시보드에서 에이전트 마지막 하트비트 시간이 표시되지 않습니다.
> - 이 PR은 에이전트 카드에 마지막 활성화 시간을 표시하는 UI를 추가합니다.
> - 덕분에 관리자가 유휴 또는 오류 상태 에이전트를 즉시 파악할 수 있습니다.

## 코드 스타일

- TypeScript 엄격 모드 (`strict: true`)
- ESLint + Prettier 설정을 따릅니다.
- 커밋 전 `pnpm lint` 실행을 권장합니다.

## 보안 관련 기여

보안 취약점을 발견한 경우 공개 이슈를 생성하지 마십시오.  
[GitHub Security Advisories](https://github.com/wogho/OpenMento/security/advisories)를 통해 비공개로 신고해 주세요.
