---
title: Institutions API
description: 기관(Institution) 관련 REST API
---

## 기관 목록 조회

<ParamField query="page" type="number">
  페이지 번호 (기본값: 1)
</ParamField>

<ParamField query="limit" type="number">
  페이지당 항목 수 (기본값: 20, 최대: 100)
</ParamField>

```bash
GET /api/institutions
```

## 기관 단건 조회

```bash
GET /api/institutions/:id
```

<ParamField path="id" type="string" required>
  기관 UUID
</ParamField>

## 기관 생성

```bash
POST /api/institutions
```

```json
{
  "name": "코딩 아카데미",
  "slug": "coding-academy",
  "settings": {}
}
```

## 기관 수정

```bash
PATCH /api/institutions/:id
```

<Note>
모든 요청은 관리자 권한이 필요합니다. 요청 헤더에 `Authorization: Bearer <token>`을 포함하세요.
</Note>
